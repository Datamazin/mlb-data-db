-- =============================================================================
-- Gold 009 — league_averages
-- M6 stub: requires silver.fact_batting which is populated when
-- /v1/people/{id}/stats bronze extraction is added (M6).
-- TODO(M6): compute weighted batting averages per league+season+game_type.
-- =============================================================================

SELECT 1 WHERE 1 = 0;
